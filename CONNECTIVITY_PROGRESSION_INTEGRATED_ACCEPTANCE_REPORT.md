# Connectivity + Progression 통합 수용성 감사

## 1. 감사 범위

동결된 Connectivity v2와 Progression 평가기 및 정적 웹 표시를 함께 점검했다. 전술 기능, 역할 지식, 점수, Connectivity 엔진은 변경하지 않았다. 표시 계층에서만 연결 툴팁이 내부 semantic/evidence ID를 노출하던 문제를 한국어 축구 용어와 일반적인 근거 문구로 교체했다.

## 2. 경계 검증

- **Connectivity**는 구성된 위치 사이에 구조적 연결 후보가 있는지, 지역별 경로군·지원/순환·의존도를 설명한다.
- **Progression**은 같은 구조 링크를 따라 점유한 세로 라인을 어디까지 전진하는지, 중간 점유 라인 건너뜀·전진 후 정체·전진 의존도를 설명한다.

두 섹션은 같은 링크를 참조할 수 있으나, 전자는 연결의 존재와 대안 구조, 후자는 전진 도달선과 정체를 표시한다. 점수·패스 성공률·점유율·찬스/득점 예측은 표시하지 않는다.

## 3. 7개 프리셋 교차 점검

| 프리셋 | Connectivity | Progression | 판정 |
|---|---|---|---|
| 4-2-3-1 | 좌/중/우에 복수의 구조적으로 구분되는 경로군 | 전 지역 최전방 라인까지 전진 | 일관됨 |
| 4-3-3 | 좌/중/우에 복수 경로군 | 좌·우 최전방, 중앙 공격형 미드필드 라인까지 | 일관됨 |
| 4-4-2 | 좌·우 복수 경로군, 중앙 완결 지역 경로 없음 | 좌·우 최전방, 중앙 수비 라인까지 | 일관됨: 지역 경로군의 완결성과 해당 지역에서의 전진 도달선은 다른 관측값 |
| 4-2-4 | 좌·우 복수 경로군, 중앙 완결 지역 경로 없음 | 좌·우 최전방, 중앙 수비 라인까지 | 일관됨 |
| 3-4-2-1 | 전 지역 복수 경로군 | 전 지역 최전방 라인까지 | 일관됨 |
| 3-4-3 | 전 지역 복수 경로군 | 좌·우 최전방, 중앙 윙백 라인까지 | 일관됨 |
| 3-5-2 | 전 지역 복수 경로군 | 전 지역 최전방 라인까지 | 일관됨 |

형태의 우열이나 전술 성과를 결론내리지 않았고, 중복된 결론 문구도 추가하지 않았다.

## 4. 지역·하이라이트 상호작용

좌/중/우 카드가 서로 다른 평가 목적을 유지한다. Connectivity 지역 카드를 누르면 Progression 대표 경로 선택을 해제하고, Progression 카드를 누르면 Connectivity 지역 선택을 해제한다. 두 의존도 버튼도 반대 분석의 선택 상태를 해제한다. `clear`, 분석 실행, 포메이션 변경, 역할 변경, 표시 모드 변경은 Progression 경로·의존 위치를 모두 초기화한다. 따라서 이전 화살표·노드·요약 상태가 다음 분석에 남지 않는다.

## 5. 포메이션·역할 변경 안전성

구조 링크와 Progression 기본 토폴로지는 구성된 위치에서만 만들어진다. 검증된 역할 semantic은 역할 보정 설명에만 반영되며, 표시 코드가 구조 경로를 새로 만들거나 없애지 않는다. 7개 프리셋의 동결 링크/경로 수 회귀와 역할 evidence 부재 시 토폴로지 불변 테스트가 이를 확인한다.

## 6. 의존도·공유 연결자·정체 경계

Connectivity 의존도와 Progression 의존도는 별도 평가기 결과이며 같은 뜻으로 표시하지 않는다. 공유 연결자는 평가기가 별도 생존성 손실을 입증하지 않는 한 병목/의존도로 승격되지 않는다. Progression의 `advance_then_stall`은 지원·순환 가능 여부를 맥락으로만 보존하며, 구조적 단절이라고 다시 표현하지 않는다. 현재 7개 프리셋에는 전진 후 정체 항목이 없다.

## 7. 라인 건너뜀·역할 보정

라인 건너뜀은 출발과 도착 사이에 실제 점유된 세로 라인이 있을 때만 표시한다. 프리셋별 확인 수는 4-2-3-1 0, 4-3-3 4, 4-4-2 0, 4-2-4 4, 3-4-2-1 4, 3-4-3 4, 3-5-2 2다. 희소한 포메이션의 빈 이름 라인을 건너뛰었다고 표시하지 않는다.

Leicester 4-2-3-1의 Progression 역할 보정은 검증된 항목만 사용한다: DML의 전방 패스, DMR의 공격형 미드필드 라인 전진, AMC의 라인 사이 수신, AML의 중앙 수신·하프스페이스 이동, AMR의 뒷공간 공격, RB의 안쪽 이동. Connectivity에는 링크/수신·연결 문맥으로, Progression에는 분배·전진 수신·이동 문맥으로만 나타난다.

## 8. UI 밀도·문구 감사

전술판은 상단의 주된 작업 영역으로 유지된다. Connectivity와 Progression은 분리된 짧은 카드, 접힌 근거/한계 블록으로 표시된다. 내부 용어 `route_profile`, `occupied_line`, `semantic`, `ERS`, `node-removal`, `dependency_type`, canonical band는 사용자 툴팁에서 노출하지 않는다. 툴팁은 위치명, 라인명, 구조적 연결과 한계만 한국어로 표시한다. 패스 성공, 점유율, 압박 해제, 찬스 창출, 위협성, 득점 가능성에 관한 비모델링 주장을 표시하지 않는다.

## 9. 합성 교차 차원 사례

기존 합성 A–L fixture로 연결만 있고 유의미한 전진이 없는 상태, 연속 전진, 중간 라인 정체, 여러 Connectivity 경로의 하나의 Progression profile, 의존도, 공유 연결자, 희소 구조를 검증했다. 프레젠터는 평가기 출력만 사용하며 점수나 원시 경로 수를 제목으로 만들지 않는다.

## 10. 회귀 결과

Connectivity 동결 기준은 유지됐다.

| 프리셋 | 구조 링크 / 전진 경로 |
|---|---|
| 4-2-3-1 | 38 / 16 |
| 4-3-3 | 40 / 48 |
| 4-4-2 | 42 / 32 |
| 4-2-4 | 38 / 32 |
| 3-4-2-1 | 42 / 25 |
| 3-4-3 | 38 / 20 |
| 3-5-2 | 48 / 40 |

Progression은 점유 라인 이득, 점유 라인만의 실제 건너뜀, 지역별 최고 도달선, 전진 후 정체, 전진 의존도, 역할 보정을 계속 별도 출력한다. Python↔JavaScript parity runner와 정적 프레젠터 runner가 같은 결과를 확인했다.

## 11. 변경 파일

- `web/static/app.js`: 연결선 툴팁에서 내부 ID를 제거하고 사용자용 위치/라인 문구로 교체.
- `test_progression_user_analysis_ui.py`: 툴팁의 내부 ID 비노출 회귀 검증 추가.
- `test_connectivity_progression_integrated_acceptance.py`: 7개 프리셋의 동결 토폴로지·평가 어휘·점유 라인 건너뜀 경계 회귀 검증 추가.
- `CONNECTIVITY_PROGRESSION_INTEGRATED_ACCEPTANCE_REPORT.md`: 본 감사 기록.

## 12. 실행 결과와 Git 상태

전체 unittest **229 passed**, `python fm26lab.py self-test` 통과, Python↔JavaScript parity 통과, 정적 프레젠터 runner 테스트 통과, JavaScript syntax check 통과, `git diff --check` 통과를 확인했다. Git commit과 push는 수행하지 않았다.

READY_TO_FREEZE_PROGRESSION
