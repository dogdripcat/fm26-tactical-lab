services:
  - type: web
    name: fm26-tactical-lab
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT web.app:wsgi_application
    healthCheckPath: /api/health
